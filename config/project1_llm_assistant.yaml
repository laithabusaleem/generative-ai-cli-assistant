model:
  # Default: real OpenAI model for public users.
  # Requires OPENAI_API_KEY to be set in the environment.
  provider: openai
  name: gpt-4.1-mini
  temperature: 0.7
  max_tokens: 512

runtime:
  # Max turns in a single CLI session
  max_turns: 20

logging:
  level: info
